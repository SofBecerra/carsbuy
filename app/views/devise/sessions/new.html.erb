<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Inicia sesión</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: url("https://img.freepik.com/fotos-premium/vista-frontal-coche-negro-moderno-generico-marca_110488-530.jpg?w=996") no-repeat center center fixed;
      background-size: cover;
    }
    .form-container {
      background: rgba(255, 255, 255, 0.8); /* Fondo blanco con transparencia */
      border-radius: 15px;
      padding: 2rem;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2); /* Sombra elegante */
    }
    h2 {
      color: #343a40; /* Color gris oscuro */
      font-weight: 700;
      margin-bottom: 1.5rem;
    }
    .btn-primary {
      background-color: #000; /* Botones negros */
      border-color: #000; /* Botones negros */
      border-radius: 5px; /* Esquinas levemente redondeadas */
      padding: 0.5rem 1.5rem; /* Espaciado interno del botón */
    }
    .btn-primary:hover {
      background-color: #333; /* Color negro oscuro al pasar el mouse */
      border-color: #333; /* Color negro oscuro al pasar el mouse */
    }
    .form-control {
      border-radius: 5px; /* Esquinas levemente redondeadas */
    }
    .form-group label {
      font-weight: 500;
      color: #495057;
    }
    .text-center {
      font-size: 1.2rem; /* Tamaño de texto ligeramente mayor */
      color: #6c757d;
    }
    @media (max-width: 576px) {
      .col-md-6 {
        padding: 0 15px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="row justify-content-start"> <!-- Cambiar justify-content-end a justify-content-start -->
      <div class="col-md-6">
        <div class="form-container my-5">
          <h2 class="text-center mb-4">Inicia sesión</h2>
          <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <%= f.error_notification %>
            <div class="form-inputs">
              <div class="form-group">
                <%= f.input :email,
                            required: true,
                            autofocus: true,
                            input_html: { autocomplete: "email", class: "form-control" },
                            label: "Correo electrónico",
                            placeholder: "Ingresa tu correo electrónico" %>
              </div>
              <div class="form-group">
                <%= f.input :password,
                            required: true,
                            input_html: { autocomplete: "current-password", class: "form-control" },
                            label: "Contraseña",
                            placeholder: "Ingresa tu contraseña" %>
              </div>
            </div>
            <div class="form-actions text-center">
              <%= f.button :submit, "Inicia sesión", class: "btn btn-primary btn-block" %>
            </div>
          <% end %>
          <%= render "devise/shared/links" %>
        </div>
      </div>
    </div>
  </div>
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
